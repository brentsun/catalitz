import * as syntax from "./syntax";
export = Main;
declare module Main {
    const themeFiles: string[];
    interface Options {
        dir: string;
        resources: {
            productName: string;
            copyright?: string;
            productDescription?: string;
        };
        themeName?: string;
        writeFile?: (path: string, content: string) => void;
    }
    function generate(elements: syntax.Element[], options: Options): void;
}
